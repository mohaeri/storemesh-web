import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('packaging UI exposes Finished Goods, manager reopen and physical unscan',async()=>{const source=await readFile(new URL('../app.js',import.meta.url),'utf8');for(const token of['warehouseLocation','data-finished-goods','/finished-goods','data-carton-unscan','/scans/remove','override:approve','data-package-action="reopen"'])assert.ok(source.includes(token),`missing ${token}`)});
