import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';
test('web hides role creation and credential controls when JWT permissions do not authorize them',async()=>{const source=await readFile(new URL('../app.js',import.meta.url),'utf8');assert.match(source,/if\(!can\('roles:create'\)\).*role-create.*remove/);assert.match(source,/if\(!can\('credentials:issue'\)\|\|!canManageUser\(user\)\)button\.remove/);assert.match(source,/role\.permissions.*permission==='\*'\|\|!can\(permission\)/);assert.match(source,/if\(!canManageUser\(user\)\)button\.remove/)});
