import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';
test('inventory UI requests adjustments and exposes a permission-gated approval queue',async()=>{const source=await readFile(new URL('../app.js',import.meta.url),'utf8');assert.match(source,/data-action="inventory-adjustment-request"/);assert.match(source,/can\('inventory:adjust\.approve'\)/);assert.match(source,/data-adjustment-action="approve"/);assert.match(source,/data-adjustment-action="reject"/);assert.match(source,/\/api\/inventory-adjustments\/\$\{b\.dataset\.adjustment\}/)});
