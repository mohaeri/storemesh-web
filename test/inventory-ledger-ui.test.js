import test from 'node:test';import assert from 'node:assert/strict';import{openInventoryLedgerDialog}from'../inventory-ledger-dialog.js';
test('inventory drill-down fetches and renders ordered quantity history',async()=>{let requested;const dialog={innerHTML:'',querySelector:()=>({onclick:null}),showModal(){this.shown=true},close(){},remove(){}},document={createElement:()=>dialog,body:{append(){}}};await openInventoryLedgerDialog({document,item:{id:'batch-1',code:'B-1'},apiBase:'http://site.test',token:'reader',fetchImpl:async url=>{requested=url;return{ok:true,json:async()=>({items:[{reason:'BATCH_RECEIVED',beforeQty:0,delta:4,afterQty:4,occurredAt:'2026-09-01T10:00:00Z'}]})}}});assert.equal(requested,'http://site.test/api/inventory/BATCH/batch-1/ledger');assert.equal(dialog.shown,true);for(const token of['B-1','BATCH_RECEIVED','تاریخچه موجودی'])assert.match(dialog.innerHTML,new RegExp(token))});
