import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';
test('inventory UI supports routine movement location correction and manager emergency relocation',async()=>{const source=await readFile(new URL('../app.js',import.meta.url),'utf8');for(const token of['data-action="inventory-item-move"','/api/inventory-items/${itemType}/${itemId}/move','data-action="location-correction"','LOCATION_CORRECTION','تأیید مدیر دوم','data-action="emergency-relocation"','/emergency-relocate','can(\'override:approve\')'])assert.ok(source.includes(token),`missing ${token}`)});
