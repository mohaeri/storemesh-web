import test from'node:test';import assert from'node:assert/strict';import{readFile}from'node:fs/promises';test('supplier create and edit UI exposes optional contact region and settlement fields',async()=>{const source=await readFile(new URL('../app.js',import.meta.url),'utf8');for(const name of['contactName','contactPhone','contactEmail','region','settlementReference'])assert.equal(source.includes(name),true);assert.match(source,/data-supplier-fields/);assert.match(source,/master-data:write/)});
